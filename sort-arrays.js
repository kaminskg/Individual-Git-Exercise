// importing the selection sort from basicSort.js
const sort = require('./basicSort')
let nums = [18, 12, 72, 99, 4, 1]
let words = ["Bob", "Charlie", "Alpha", "Zulu", "Yankee", "Xray"]
console.log(sort(nums))
console.log(sort(words))
